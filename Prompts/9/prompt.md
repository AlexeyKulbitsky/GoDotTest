Take the existing project as your starting point. I want you to upgrade the two existing enemy types and introduce a third one. No new scene architecture is needed - this task is purely about improving and extending enemy behavior using what is already in place. Starting with the chaser enemy. Right now it stands completely still when the player is out of range. I want it to patrol instead. When the player is outside the detection radius the chaser should walk back and forth between two points defined relative to its starting position. The patrol range should be an exported variable so it can be tuned per instance from the editor. When the player enters the detection radius the chaser should stop patrolling and switch immediately into chase mode exactly as it does now. When the player leaves the detection radius the chaser should return to patrolling from wherever it currently is rather than snapping back to its starting posotion. The transition between patrol and chase should be smooth - no teleporting, no speed spike, no jitter at the moment of switching. The patrol speed and chase speed should be separate exported variables since it makes sense for the chaser to move slower while patrolling than while actively chasing. Moving on to the shooter enemy. Between shots there should be a clear visual indication that the enemy is reloading - change the shooter's color briefly during the cooldown window and revert it back when it is ready to fire again. The color during reload and the color during ready state should both be exported variables so they can be distinguished clearly in the editor. No animation system is required - a simple color swap on the ColoRect is enough. Everything else about the shooter stays the same. The third enemy type is a bouncer. Save it as enemy_bouncer.tscn with its script enemy_bouncer.gd. The bouncer sits on a platform and jumps repeatedly at a fixed interval, lands back on the same platform, and repeats indefinitely. It does not move horizontally at all - it only moves vertically. When the player makes contact with the bouncer from the side or from above while it is mid-air or grounded, the player loses a life and the normal respawn flow triggers. Howener if the player is powered up the bouncer dies instead, exactly the same as the other enemy types. The jump force and the interval between jumps should both be exported variables. The bouncer should use CharacterBody2D with gravity applied so its jump and landing behave like real physict rather than a scripted position animation. Place one instance of the boucer in each level in a position where its vertical movement creates a meaningful obstacle for the player. For all three enemy types, make sure the behavior introduced or modified in this task is consistent with the existing systems - lives are deducted through the game manager, the power-up kill behavior works correctly on the bouncer, and all enemies reset to their starting positions and states when the player dies or restarts, including the chaser returning to patrol mode and the bouncer resetting its jump timer.