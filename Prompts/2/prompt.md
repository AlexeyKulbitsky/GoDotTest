Hey! This is a follow-up task to the platformer prototype you built previously. It should take you around 1.5 to 2 hours. We are continuing the Godot 4.x engine evaluation, so the same rules apply — clean code, clear naming, no leftover debug statements.

Take the existing project as your starting point. Your goal is to refactor the platform setup, introduce three new platform types each with its own behavior, and add a camera that follows the player.

First, extract the existing static platforms from `platformer.tscn` into their own reusable scene. Create a `platform_static.tscn` file with a StaticBody2D as the root and attach a script named `platform_static.gd` to it even if the script stays minimal for now. This gives all platform types a consistent structure going forward.

The first new platform type is a lift. Create it as `platform_lift.tscn` with a script `platform_lift.gd`. When the player steps onto this platform, it should start moving upward at a steady speed. It should stop after traveling a set distance from its starting position. Once it reaches the top, it stays there — it does not return. Use an AnimatableBody2D as the root node for this and all other moving platform types so that the player rides the platform correctly without slipping off.

The second new platform type is a horizontally moving platform. Create it as `platform_moving.tscn` with a script `platform_moving.gd`. This platform moves left and right repeatedly between two points at a constant speed. The movement should be smooth and loop indefinitely regardless of whether the player is on it or not. Define the travel distance and speed as exported variables so they can be adjusted per instance directly in the editor.

The third new platform type is a catapult platform. Create it as `platform_catapult.tscn` with a script `platform_catapult.gd`. When the player steps onto it, it launches the player upward with a strong vertical velocity boost — stronger than a regular jump. The platform itself does not move. After triggering, it should go into a brief cooldown of around 2 seconds before it can catapult again, in case the player lands on it a second time. To apply the velocity boost, emit a signal from the platform and handle it in `player.gd` by exposing a method that accepts an external vertical impulse.

For detecting when the player steps onto a platform, use the `body_entered` signal on an Area2D child node placed on top of each moving or interactive platform. Make sure the Area2D is sized to match the platform surface so it does not trigger from the sides or below.

Add a Camera2D as a child node of the player in `player.tscn`. This way the camera automatically follows the player throughout the level without any additional scripting. Set the camera's zoom to a reasonable value like 0.75 so the player can see a comfortable amount of the level at once. Enable drag smoothing on the camera with a moderate smoothing speed so the movement feels fluid rather than snapping rigidly to the player position. Since the level will now extend vertically to accommodate the new platforms, make sure the overall layout in `platformer.tscn` is tall enough for the camera to have meaningful space to scroll through.

Update `platformer.tscn` to instance one of each new platform type alongside the existing static platforms. Position them so the level feels like a natural vertical progression — the moving platform somewhere in the lower section, the lift in the middle, and the catapult near an area where the extra height is useful to reach a collectable. Make sure all collectables and platforms remain reachable and the level is completable from start to finish.

All new scene files go in the same project root as the existing ones. Exported variables should have sensible defaults so the scenes work out of the box when instanced without any manual configuration. Ping me if anything about the platform interaction with the player feels unclear.